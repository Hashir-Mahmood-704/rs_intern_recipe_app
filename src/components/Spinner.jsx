import {Circles} from "react-loader-spinner"

const Spinner = () => {
    return <div className="flex justify-center w-full">
        <Circles color="#FFA500" height={60} width={60}/>
    </div>
}

export default Spinner